var add = (num1, num2) => { return (num1 + num2) };
var sub = (num1, num2) => { return (num1 - num2) };
var mul = (num1, num2) => { return (num1 * num2) };
var div = (num1, num2) => { return (num1 / num2) };

module.exports = {add,sub,mul,div};